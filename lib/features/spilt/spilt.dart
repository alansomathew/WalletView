import 'package:flutter/material.dart';

class SpiltScreen extends StatelessWidget {
  const SpiltScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('SpiltScreen'),
      ),
    );
  }
}